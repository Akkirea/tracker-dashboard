import React from "react";
import ReactDOM from "react-dom/client";
import TrackerDashboard from "./Tracker-dashboard.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TrackerDashboard />
  </React.StrictMode>
);
